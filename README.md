# config

Personal public configuration files.

## Directory Layout

- `subconverter/mobile-clash/` - external `subconverter` INI configs for generating Clash/Mihomo mobile profiles.

## Safety

Do not commit subscription URLs, server IPs, account tokens, passwords, or private service endpoints.
